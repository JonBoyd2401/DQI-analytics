# ADR 001: Modular monolith

**Status:** Accepted. The MVP uses separately runnable web/API applications with shared packages, avoiding premature distributed-system overhead while preserving module boundaries and worker extraction paths.
