# ADR 008: Elasticsearch/OpenSearch compatibility

**Status:** Accepted. Both backends implement a connector capability contract and compile from shared IR. Backend-specific behaviour must be contract-tested by supported version.
