# ADR 003: Backend-neutral query IR

**Status:** Accepted. Authorised semantic IDs compile into a versioned IR. Only deterministic backend adapters produce executable DSL, preventing direct execution of model output.
