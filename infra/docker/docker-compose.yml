services:
  api:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    command: npm run start -w @cx/api
    ports: ["3001:3001"]
    environment: { HOST: "0.0.0.0", PORT: "3001" }
